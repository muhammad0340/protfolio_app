import 'package:flutter/material.dart';

class LetsSocializeScreen extends StatelessWidget {
  const LetsSocializeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("muhammad arsalan"),
    );
  }
}
