import 'package:flutter/material.dart';

class TictacteoAppScreen extends StatelessWidget {
  const TictacteoAppScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("this is tictacteo app screen"),
    );
  }
}
